var intNumber = 42,
    floatNumber = 3.14159,
    trueCondition = true,
    falseCondition = false,
    word = "Howdy",
    nothing = null,
    undefinedValue;

jsConsole.writeLine("Numbers: " + intNumber + ", " + floatNumber);
jsConsole.writeLine("Logical (Boolean): " + trueCondition + " / " + falseCondition);
jsConsole.writeLine("Strings: " + word);
jsConsole.writeLine("null: " + nothing);
jsConsole.writeLine("undefined: " + undefined);