var intNumber = 42,
    floatNumber = 3.14159,
    trueCondition = true,
    falseCondition = false,
    word = "Howdy",
    nothing = null,
    undefinedValue;

jsConsole.writeLine(intNumber + " is " + typeof (intNumber));
jsConsole.writeLine(floatNumber + " is " + typeof (floatNumber));
jsConsole.writeLine(trueCondition + " is " + typeof (trueCondition));
jsConsole.writeLine(falseCondition + " is " + typeof (falseCondition));
jsConsole.writeLine(word + " is " + typeof (word));
jsConsole.writeLine(nothing + " is " + typeof (nothing));
jsConsole.writeLine(undefinedValue + " is " + typeof (undefinedValue));